# v2 ownership fixture workspace

Copied per run by rsi.ts. `plans/active.sdd.md` is a pending legacy SDD that owns the package named `feature-b-pkg` (`packages/feature-b`); `plans/shipped.sdd.md` shipped and owned `packages/feature-a`.
