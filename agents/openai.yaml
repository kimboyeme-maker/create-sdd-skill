interface:
  display_name: "Create SDD"
  short_description: "Create, merge, refactor, or audit repository SDDs"
  default_prompt: "Use $create-sdd to write an implementation-ready SDD and hand it directly to my coding host."
